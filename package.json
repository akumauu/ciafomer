{
  "name": "ciallo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && cd src-tauri && cargo run"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
